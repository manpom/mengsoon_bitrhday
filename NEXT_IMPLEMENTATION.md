# 다음 작업 인수인계 — 반드시 읽기

작성: 2026-09-05

## 사용자 검토 기준 (미완료 — 다음 작업에서 완료해야 함)

1. **맹돌이**
   - 게임에 최종 반영하기 전에, 8방향 × 방향당 10프레임(총 80프레임)으로 완성한다.
   - 정지 프레임과 걷기 프레임은 **동일한 맹돌이 모델/의상/조명/비율**이어야 한다.
   - 기존 맹돌이 콘셉트: 노란 맹꽁이, 하늘색 후드티, 흰 후드끈, 크림색 배, 분홍 볼.
   - **가방·백팩·어깨끈은 절대 넣지 않는다.**
   - 프레임 경계, 잘린 발, 다른 프레임 잔상, 겹친 팔다리가 보이면 적용하지 않는다.
   - 이미지 생성은 사용자의 요청대로 Chrome의 ChatGPT/Work 브라우저 생성으로 진행한다. 기존 `mengdol_down_idle.png`를 레퍼런스로 직접 첨부해 동일성을 고정한다.
   - 적용 전: 정지(8방향) + 걷기(8방향 중 최소 앞/뒤/좌/우/대각 4방향) 스크린샷/실행으로 검수한다.

2. **방 구조: 배경과 소품을 반드시 분리**
   - 방을 완성된 배경 한 장에 가구까지 굽는 방식으로 만들지 않는다.
   - 배경에는 바닥·벽·창문빛처럼 상호작용하지 않는 것만 둔다.
   - 침대, 복싱글러브, 그리고 책장·찻상·플로어램프·화분 등 4~5개 소품은 **개별 PNG 에셋 + 개별 Godot 노드/씬**이어야 한다.
   - 각 소품은 별도 Sprite2D, StaticBody2D/CollisionShape2D, 조사 범위, 대사, 느낌표를 가진다.
   - Player와 소품은 같은 Y-sort 월드 아래에 배치한다. 맹돌이가 소품 뒤를 지나면 가려지고, 아래쪽으로 나오면 앞으로 보여야 한다.
   - 소품 충돌로 관통이 막혀야 한다. 침대와 책장처럼 큰 소품을 밟고 지나가면 안 된다.
   - 새 독립 배경 후보: `assets/sprites/stages/room_empty_diorama.png`
   - 새 소품 시트 후보: `assets/sprites/props/room_props_sheet.png` (침대/글러브/책장/찻상/램프/화분). 반드시 각 PNG로 분리하고 투명도·크롭을 검수한 뒤 적용한다.

3. **표시/UI 품질**
   - 느낌표를 단순 Label 텍스트로 만들지 않는다.
   - 현재 방/소품과 같은 몽글몽글한 3/4 장난감 디오라마 미감의 독립 이미지 에셋으로 생성한다.
   - 일반 조사 소품은 흰/크림 계열, 미니게임 시작 복싱글러브는 눈에 띄는 노란 계열이다.
   - 가벼운 부유·펄스 애니메이션을 적용하고, 소품 윗부분에 자연스럽게 표시한다.

4. **그림자**
   - 맹돌이 발밑에 부드러운 타원 그림자를 둔다.
   - 이동 중에도 캐릭터 발 위치를 따라가야 한다.
   - 소품 접지 그림자와 색감/부드러움이 조화되어야 한다.

5. **오프닝/조사 흐름**
   - `도로롱... 도로롱...` → `으음...` → 침대에서 내려옴 →
     `여기가... 어디지?` → `내 집인가? 기억이 나질 않아.` → `주위를 둘러봐야겠어.`
   - 오프닝 이후 조사 가능한 독립 소품 위에 느낌표가 뜬다.
   - 복싱글러브 조사 시 미니게임 1로 진입한다.

6. **iPhone 18 Pro / Web**
   - Godot Web Export → Safari 가로 모드 기준.
   - `gl_compatibility`, `canvas_items`, `expand`, Linear 필터 유지.
   - Web Export에서 Thread Support/Extensions Support 끄기.
   - 실제 export 결과는 `build/web/index.html`에 한 번 생성됐으나, 최종 소품/캐릭터 반영 후 반드시 다시 내보내고 Safari 실기기에서 터치·폰트·오디오·프레임률을 확인한다.

## 현재 상태 주의

- 현재 `scripts/room.gd`에는 이전 실험 코드와 임시 소품 동적 생성이 남아 있다. 이를 최종 구조로 간주하지 말 것.
- 현재 맹돌이 8방향 시트도 **최종 승인 전 후보**다. 프레임 경계/동일성 검수를 통과한 새 세트로 교체한 후 게임에 반영한다.
- 독립 소품 PNG 중 일부는 분리 저장 과정에서 실패했을 수 있다. 파일을 그대로 믿지 말고 투명도·크기·렌더링을 먼저 확인한다.
- 다음 작업의 완료 조건은 `room.tscn` 실제 실행 화면에서 분리된 소품, Y-sort, 충돌, 조사, 고품질 느낌표, 일관된 8방향 맹돌이가 모두 확인되는 것이다.
